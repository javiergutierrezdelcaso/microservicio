# ─────────────────────────────────────────────
  # JOB 0: Obtención dinámica de IPs desde Terraform Cloud
  # ─────────────────────────────────────────────
  resolver-ips:
    name: Obtener IPs desde Terraform Cloud
    runs-on: ubuntu-latest
    # Forzamos un environment para que GitHub Actions libere el secreto TF_API_TOKEN
    environment: pro
    outputs:
      ip_pre: ${{ steps.get_ips.outputs.ip_pre }}
      ip_pro: ${{ steps.get_ips.outputs.ip_pro }}
    steps:
      - name: Consultar API de Terraform Cloud
        id: get_ips
        env:
          TF_TOKEN: ${{ secrets.TF_API_TOKEN }}
        run: |
          echo "Iniciando consulta simplificada a Terraform Cloud..."
          
          # 1. Obtener IP de PRE (Usando la ruta directa por nombre de Workspace)
          echo "Solicitando IP de PRE..."
          IP_PRE=$(curl -s \
            --header "Authorization: Bearer $TF_TOKEN" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/organizations/ecoanalyzer-org/workspaces/Infraestructura_Pre/current-state-version-outputs" \
            | jq -r '.data[] | select(.attributes.name == "vm_public_ip") | .attributes.value // empty' | tr -d '"')

          # 2. Obtener IP de PRO
          echo "Solicitando IP de PRO..."
          IP_PRO=$(curl -s \
            --header "Authorization: Bearer $TF_TOKEN" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/organizations/ecoanalyzer-org/workspaces/Infraestructura_Pro/current-state-version-outputs" \
            | jq -r '.data[] | select(.attributes.name == "vm_public_ip") | .attributes.value // empty' | tr -d '"')

          # Validaciones e impresión de respuestas para depuración
          echo "Resultados obtenidos:"
          echo "-> IP PRE: [${IP_PRE}]"
          echo "-> IP PRO: [${IP_PRO}]"

          if [ -z "$IP_PRE" ] || [ -z "$IP_PRO" ]; then
            echo "❌ Error Crítico: No se pudo obtener una o ambas IPs. Verifica que el Token sea correcto y que el Output 'vm_public_ip' exista en Terraform Cloud."
            exit 1
          fi

          # Guardar outputs de GitHub Actions
          echo "ip_pre=${IP_PRE}" >> $GITHUB_OUTPUT
          echo "ip_pro=${IP_PRO}" >> $GITHUB_OUTPUT
          echo "✅ IPs guardadas en el output del pipeline con éxito."
